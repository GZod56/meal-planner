{
  "name": "meal-planner",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.0",
    "cheerio": "^1.1.2"
  },
  "engines": {"node": ">=22"}
}
